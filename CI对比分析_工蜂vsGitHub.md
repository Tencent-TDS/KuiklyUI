# 工蜂 Stream CI vs GitHub Actions：AI 便捷度对比分析

> **状态：阶段性快照（2026-08-08）。两边 CI 已收敛，真机线已启用并全绿。**
> - GitHub 线：模拟器 job 连续多轮全绿；**真机 job(`e2e-device`) 已启用并跑通，TC1-TC10 全绿**。
> - 工蜂线：实跑全绿过，但已停用（真机回归由 GitHub self-hosted 真机线承接）。
>
> 本文的定位是**把两条线收敛过程的对照数据固化下来**，供后续维护参考。
> 结论部分标注了置信度；真机线跑通后，此前低置信度的项已复核。

---

## 精简版

- **总体：GitHub 明显占优**，但优势**不在"少踩坑"，而在"每踩一个坑的人工成本"**。
- 最反直觉的一点：**GitHub 轮次更多（13 轮 vs 工蜂 7 轮），人工介入却少得多。**
  所以「轮次」不是便捷度的有效指标，**「每轮的人工介入次数」才是**。
- 核心差异是**可观测性**：工蜂日志落本机 `/tmp`，AI 读不到 → 每轮都要人贴日志；
  GitHub 日志进云端 job log，AI 用 `gh run view --log` 自取 → 人只在决策点介入。
- 工蜂唯一硬优势是**真机 1:1 保真度**——这属于测试质量维度，不属于 AI 便捷度。
  且它恰恰**反向拖累**便捷度（真机证据对 AI 不可见）。

---

## 1. 硬数据

| 指标 | 工蜂 Stream CI | GitHub Actions | 说明 |
|---|---|---|---|
| 收敛所需 commit | 15 | 18 | GitHub 略多 |
| 失败轮次 | 7 | 13 | GitHub 近两倍 |
| 时间跨度 | 10:41 → 19:19（约 8.6h） | 08-07 17:05 → 08-08 15:08（约 22h，含隔夜） | 不可直接比 |
| **每轮人工介入** | **每轮必须贴日志** | **仅决策点介入** | ← 关键差异 |
| 日志可及性 | 本机 `/tmp`，AI 读不到 | `gh run view --log` 直读 | |
| 失败证据 | 本机目录，需人工翻找 | artifact 下载（含截图/view-tree/logcat） | |
| 单轮耗时（收敛后） | 本地复跑 1m54s | 4m24s ~ 10m29s | 工蜂无云端排队 |
| 环境保真度 | 真机 1:1 | 模拟器近似 | 工蜂胜 |

> 注：轮次统计口径 = 一次失败到下一次修复推送。GitHub 侧含 4 次
> `cancelled`（3 次是 tail 挂死导致的 60min timeout、1 次我主动取消），
> 这些"轮次"其实没产生新信息，**扣掉后有效轮次约 9 轮**。

---

## 2. 为什么 GitHub 轮次更多却更省人力

### 2.1 轮次多的真实原因（不是 GitHub 更差）

1. **GitHub 线独有的环境坑更多**：云端 runner 是黑盒，多一层
   「action 内部命令 + runner 进程组清理」风险。工蜂 Mac runner 直接可控。
2. **无 KVM 权限这个总根因隐藏得深**：它让 7 轮症状各不相同（详见
   `GitHubCI失败复盘.md`），本质是 1 个问题被拆成 7 轮才识别出来。
   工蜂那 7 轮是 7 个**真正独立**的问题。
3. **工蜂线的经验被 GitHub 线复用了**：GitHub 前两轮的 install 抖动，
   直接套用工蜂已沉淀的「清残留 + 重启 adb 复跑」结论，没有重新排查。
   **反向复用没发生过**（工蜂在前）。

### 2.2 人力成本的实际差异

**工蜂的典型一轮**：
```
CI 失败 → 人打开 Stream 页面 → 下载/复制日志 → 贴给 AI
       → AI 分析 → 给修复 → 人应用并推送 → 等下一轮
                    ↑ 人工介入 3 次
```

**GitHub 的典型一轮**：
```
CI 失败 → AI 自己 gh run list 发现 → 自己拉 log → 自己定位
       → 自己改 → 自己推 → 自己等下一轮
                    ↑ 人工介入 0 次（仅在决策点被问）
```

本轮实测：从「接手」到「连续两轮绿」，中间 6 次 CI 失败**全部由 AI 自主完成
发现→取证→定位→修复→推送**，人只在三处介入：
① 确认治标修复的去留 ② 确认分支策略 ③ 跑需要密钥/交互的命令。

---

## 3. 分维度评估

### 3.1 可观测性（GitHub 压倒性胜出，置信度：高）

| | 工蜂 | GitHub |
|---|---|---|
| 主日志 | Stream 页面，需人工复制 | `gh run view --job=<id> --log` |
| Appium/引擎日志 | 本机 `/tmp/*.log`，AI 读不到 | `tail -F` 进 step stdout，随 job log 一起可读 |
| 失败截图 | 存本机，需人工翻 | artifact 下载，14 天保留 |
| view-tree/logcat | 同上 | 同上 |

**这是工蜂 7 轮排障的根因之一**：`358083fb`（失败诊断加 appium 日志尾部）、
`fd8be980`（保存原生 UI 树证据）两个 commit 都是在补"让人能看见"，
而在 GitHub 上这是默认能力。

### 3.2 配置成熟度（GitHub 胜出，置信度：高）

- **GitHub**：标准 YAML，生态 action 齐全（`android-emulator-runner`、
  `upload-artifact`、`cache` 开箱即用）。文档与社区案例充足。
- **工蜂**：Stream v2.0 是小众 DSL，踩过 pool-name 字符限制、
  `on` 无 per-job 条件等坑，且搜不到参考案例。

### 3.3 环境可控性（工蜂胜出，置信度：中）

- **工蜂**：本机 Mac runner，环境完全可控，出问题能直接上机看。
- **GitHub**：云端黑盒。本轮两个坑直接源于此——
  ① runner 无 KVM 权限（镜像策略，非我方可配）
  ② `reactivecircus` action 的 pre-script 崩溃会跳过整个 `script:`

  但注意：**这两个坑最终都在 AI 自主排障下解决了**，靠的是日志可读。

### 3.4 测试保真度（工蜂胜出，但与便捷度负相关，置信度：高）

真机能测到模拟器测不到的东西（真实输入法、厂商 ROM 行为、真实性能）。
本轮就有实例：**AVD 默认 320x640 挤掉调试节点**，这类问题真机不会遇到。

**但反过来**：工蜂那 13 个问题里，`uia2 冷启动首次查询必 hang`、
`vivo force-stop 对 instrumentation 不彻底`等，都是**真机特有的环境噪声**，
消耗了大量排障成本却与被测业务无关。

> 权衡：真机保真度换来的是**更高的排障成本 + 更低的 AI 可介入度**。

### 3.5 迭代速度（GitHub 略优，置信度：低）

- 工蜂：无云端排队，本地复跑 1m54s。
- GitHub：需排队 + 冷启动，4m24s ~ 10m29s。

单轮工蜂更快，但**算上"等人贴日志"的墙钟时间，GitHub 实际更快**——
这部分没有精确数据，故置信度标低。

---

## 4. 结论与建议

### 4.1 结论

1. **AI 便捷度：GitHub > 工蜂，主因是可观测性，不是坑更少。**
2. **「轮次」是误导性指标。** 该看的是"每轮人工介入次数"和
   "AI 能自主完成的排障比例"。本轮 GitHub 侧后者接近 100%。
3. **工蜂的真机保真度是真实价值，但它与 AI 便捷度冲突**——
   证据对 AI 不可见，且真机环境噪声会淹没业务问题。

### 4.1.1 真机线跑通后的补充结论（2026-08-08 晚间）

真机线在 GitHub self-hosted 上稳定跑通后，验证并强化了上面的结论：

4. **真机特有的环境噪声，在 GitHub 上也能被 AI 自主吸收**——
   真机 uia2 中途卡死（`QueryController: Could not detect idle state`）暴露了
   "引擎读路径无自愈"的隐蔽缺口（`getViewTree`/`getSnapshot` 未包 retry）。
   这个根因**从发现（日志）→ 定位（对比 tap/input 有自愈而读没有）→ 修复
   （f4a5dc62）→ 验证（双线全绿）全程 AI 自主完成，人工介入 0 次**。
   同样的根因如果发生在工蜂线，至少需要人贴日志才能开始。
5. **"引擎自愈机制对真机线是必要、对模拟器线是冗余"有了完整闭环**：
   早期 `7370138f`/`a7670811` 的恢复机制在模拟器（有 KVM）上从不触发，
   一度显得多余；真机线跑通后这些机制 + 新补的读路径自愈**全都被真实触发**，
   证明它们是"真机保真度的必要成本"，不是过度设计。

### 4.2 建议（供下阶段参考）

- **主线放 GitHub 模拟器**，做快速门禁（4-10min，AI 可自主排障）。
- **真机作为补充**，不做每次 push 的门禁，改为定时/手动触发，
  只跑模拟器测不到的场景（输入法、性能、厂商 ROM）。
- 若真机线要保留 AI 便捷度，**必须解决日志可及性**——
  这正是 `e2e-device` job 走 GitHub self-hosted runner 的价值：
  **日志走云端 job log，AI 能直读**，等于把工蜂的最大短板补上。

  > **已验证（2026-08-08，run 31246067342 / 31246330632）**：
  > self-hosted 真机 job 的日志确实完整进云端 job log——
  > 首跑失败时 AI 用 `gh api .../jobs/<id>/logs` 直接读到 790 行，
  > 一次定位到根因（`timeout: command not found`，macOS 无 GNU coreutils）；
  > 修复后 3259 行日志同样可读，且 `e2e-device-evidence` artifact（919 KB，
  > 含 10 张 1080x2400 真机截图 + 恢复分支的失败证据）正常上传。
  > **本节的核心假设成立**：self-hosted runner 把工蜂"证据只落本机、
  > AI 读不到"的短板补上了，真机线首次具备与模拟器线同等的 AI 可介入度。

---

## 5. 待补数据（本文后续要改的部分）

| 缺口 | 补齐条件 | 影响的章节 |
|---|---|---|
| ~~GitHub self-hosted 真机线的实际表现~~ | ~~`e2e-device` 启用并跑若干轮~~ | **已补齐，见 5.1 + 5.2** |
| ~~真机线在 GitHub 上的日志可及性验证~~ | ~~同上~~ | **已验证，4.2 假设成立** |
| 精确的墙钟时间对比 | 需要记录"等人贴日志"的时长 | 3.5（当前置信度低） |
| 长期稳定性（flaky 率） | 两边各跑 20+ 轮 | 全文 |

### 5.1 真机线首轮实测（2026-08-08，补充数据）

| 项 | 结果 |
|---|---|
| runner | `NIKAZHAO-MC0-vivo`，labels `[self-hosted, macOS, ARM64]` |
| 首跑 | 失败——`timeout: command not found`（macOS 无 GNU coreutils） |
| 定位方式 | **AI 自主**：`gh api .../logs` 读 790 行，一次定位，人工介入 0 次 |
| 修复后 | `PASS=10 FAIL=0 exit 0`，12m58s |
| 证据 | `e2e-device-evidence` 919 KB，10 张 1080x2400 真机截图 |

### 5.2 真机线稳定跑通 + 新根因（2026-08-08 晚间，补充数据）

真机线在 GitHub self-hosted 上稳定跑通后，暴露了**模拟器线测不到的新根因**：

| 项 | 结果 |
|---|---|
| 手动 run 首轮 | 失败——TC4-TC10 全挂（TC1-3 PASS） |
| 现象 | uia2 中途 idle 卡死（`QueryController: Could not detect idle state`），此后 `/view-tree`、`/page-source` 直接抛 `Could not proxy command to the remote server. timeout of 10000ms exceeded` |
| **根因** | 引擎 `getViewTree`/`getSnapshot` **未包 `withUiAutomator2Retry`**。自愈机制只覆盖了"操作"（tap/input/find），没覆盖"读"（view-tree/source/screenshot）。真机 uia2 中途卡死 → 断言路径直接抛超时、无任何恢复机会 → TC 直接 FAIL |
| 修复 | `f4a5dc62`：把 getPageSource+getWindowSize 读操作包进 `withUiAutomator2Retry`，uia2 卡死时自动 force-stop→删 session→adb relaunch→newSession 后重试；解析留在 retry 外 |
| 修复后 | **模拟器 + 真机双线 `PASS=10 FAIL=0` 全绿**。真机线三 job（单测/模拟器/真机）全 success |
| golden | 真机线 `golden-baseline/` TC1-TC10 十张 1080x2400 截图齐全 |

**新根因的意义**：
- 这是**模拟器线（有 KVM 后）从未触发、只有真机才会暴露**的恢复缺口——模拟器 uia2 稳定不卡死，所以断言路径无自愈也一直绿；真机 uia2 会中途卡死，暴露了"读路径无自愈"这个隐蔽缺口。
- **印证 3.4 的核心权衡**：真机保真度换来更高的排障成本，但正因为日志/证据对 AI 可见（4.2），这个真机特有的根因才能被 AI 自主定位并修复（人工介入 0 次）。

**两个值得记录的观察**：

1. **真机的环境噪声确实存在，且恢复机制被真实触发**：
   日志显示 `!! uia2 僵尸（findElements 卡满），快速走重建分支` →
   force-stop 重建 → 健康探测通过 → TC1-TC10 全绿。
   模拟器线（有 KVM 后）从未触发过这条路径。
   → 印证 3.4「真机保真度换来更高排障成本」，也说明
   `7370138f`/`a7670811` 这些恢复机制对真机线是必要的（对模拟器线则是冗余）。
2. **`adb install` 重试到第 3 次才成功**（`安装成功 (attempt 3)`）。
   模拟器线有 KVM 后一次即成。真机侧的 install 抖动是常态。

**耗时对比（同一 push 并行跑）**：模拟器 4m23s / 真机 12m58s。
真机慢主要在装包重试与恢复分支，不在构建（Gradle 缓存命中，18s 构建完）。

> **特别说明**：4.2 的核心建议「self-hosted runner 能补上工蜂的日志短板」
> 已通过真机线实测验证成立——首跑失败和修复后全程日志进云端 job log，
> AI 全程自主定位+修复，人工介入 0 次。
